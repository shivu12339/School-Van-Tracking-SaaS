# Flutter compile-time defines (pass via --dart-define-from-file or CI)
FLAVOR=dev
GOOGLE_MAPS_KEY=your_google_maps_key

# API (dev Android emulator → host machine)
# API_BASE_URL=http://10.0.2.2:4000/api/v1
# WS_BASE_URL=http://10.0.2.2:4000

# Firebase: place google-services.json (Android) and GoogleService-Info.plist (iOS)
# in android/app and ios/Runner after `flutter create`.
