# Test fixtures

| File | Use |
|------|-----|
| `gps.fixtures.ts` | GPS payloads, streams, teleport abuse |
| `auth.fixtures.ts` | Login bodies, tampered JWT |

Import from API tests via relative path or duplicate inline for k6 (JS).
